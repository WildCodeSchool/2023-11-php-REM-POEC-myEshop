{% extends "layout.html.twig" %}
{% block title %}Admin - Categories
{% endblock %}
{% block stylesheets %}{% endblock %}
{% block content %}
	<div class="container">
		<h1 class="text-center text-primary fw-bold col-12 col-md-6 mx-auto my-4  fw-bold bg-light border border-secondary p-4 shadow-sm">Gestion des catégories</h1>
		<div class="row my-2">
			<div class="col-12 col-md-6 mx-auto">
				<a href="/" class="btn btn-primary btn-sm mb-4">Retour</a>
				<a href="/admin/category/create" class="btn btn-success btn-sm mb-4">Ajouter une catégorie</a>
				<table class="table table-hover table-striped">
					<thead>
						<tr>
							<th>Id</th>
							<th>Nom</th>
							<th>Desciption</th>
							<th>Actions</th>
						</tr>
					</thead>
					<tbody>
						{% for category in categories %}
							<tr>
								<td>{{ category.id }}</td>
								<td>{{ category.name }}</td>
								<td>{{ category.description|slice(0,50) }}...</td>
								<td>
									<div class="d-flex">
										<a href="/admin/category/show?id={{ category.id }}" class="btn btn-info btn-sm" role="button">
											<i class="fa-regular fa-eye"></i>
										</a>
										<a href="/admin/category/update?id={{ category.id }}" class="btn btn-success btn-sm" role="button">
											<i class="fa-solid fa-pen-to-square"></i>
										</a>
										<a href="/admin/category/delete?id={{ category.id }}" class="btn btn-danger btn-sm" role="button" onclick="return confirm('Voulez-vous vraiment supprimer cette catégorie ?');">
											<i class="fa-solid fa-delete-left"></i>
										</a>
									</div>
								</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</div>
		</div>
		{% block javascripts %}{% endblock %}
	{% endblock %}
